# k1

Project knowledge for `projects/k1`. Reachable from inside the repo as
`.brain/index.md`.

Resolve against the [meta layer](../../meta/index.md) — see
[conventions](../../conventions.md) for how `Practice Override` composes.

# Overrides

*Meta practices this project narrows, extends, replaces, or suspends. Empty.*

# Patterns

*Opt-in and portable. Empty.*

# Workflows

* [prompt-length-costs-accuracy-on-fields-the-words-never-mention](workflows/prompt-length-costs-accuracy-on-fields-the-words-never-mention.md) - 1818 characters of added prompt prose cost accuracy on Part III boxes the new text never mentions; 15 characters cost nothing. Length is a variable, and the effect is not linear in it.

# Failure modes

* [a-checkbox-a-model-misreads-cannot-gate-a-check](failure-modes/a-checkbox-a-model-misreads-cannot-gate-a-check.md) - The Final K-1 checkbox read wrong on 4, 14 and 13 of the same 20 documents across prompt revisions that never mentioned checkboxes. A gating field passes its error rate straight to the false-positive rate.

# Modules

*What the major pieces are and how they fit. Empty.*

# Invariants

*Rules with an executable check attached. Empty.*

# Decisions

*Why things are the way they are. Empty.*

# Gems

*Project-local patterns worth promoting to meta. Empty.*

# Candidates for meta

Both concepts above were written proposing `meta` and were demoted on
2026-08-12 for having single-system evidence. Each names what would lift it, in a
closing section. Neither has an `instance-of` target yet because no meta concept
covers the same ground.
