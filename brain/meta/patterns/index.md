# Patterns

Opt-in. Reach for these when the problem arises; their absence is not a defect.

# Design system

* [token-architecture-three-layers](token-architecture-three-layers.md) - Scale, then intent quartets, then component semantics. The repeating role set is the load-bearing part.

# Demoted to the zamp layer, 2026-07-29

Both were held here until harvesting `tools` and `wiley` showed they are zamp
standards rather than universal ones. Left as pointers so the old paths remain
explicable:

* [theme-by-data-attribute](../../projects/zamp/patterns/theme-by-data-attribute.md) - The other repos theme by class selector, not `[data-theme]`.
* [accessibility-enforced-by-types](../../projects/zamp/patterns/accessibility-enforced-by-types.md) - Only zamp implements the union; the others expose an optional `aria-label`.
