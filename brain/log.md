---
type: Log
title: Brain bundle history
---

# Bundle history

## 2026-08-12

- **Promoted 7 concepts to `meta` and 2 to a new `projects/k1` layer, all of them about how to measure an LLM system rather than how to build one.** The batch came out of measuring a Form 1065 extraction pipeline against a synthetic corpus, a degraded-scan subset, and four real client documents. `meta/` goes from 29 concepts to 36, close enough to the ~40 index-splitting threshold in the promote skill that the next batch should split the always-on index by category instead of appending to it. The new **Practices — measurement** heading is the seam to split on.
- **[pair-both-arms-in-one-window-or-drift-picks-the-winner](meta/workflows/pair-both-arms-in-one-window-or-drift-picks-the-winner.md) was promoted the same day it was violated twice, and the promoted version says so.** The observation was written 2026-08-11. On 2026-08-12, with it sitting in the inbox, I twice compared two ledgers captured hours apart and reported large significant effects: 130 boxes fixed against 24 broken at p = 8e-19 for a rasterization change, and 197 against 15 at p ≈ 0 for a prompt edit. Run properly, the prompt edit read 0 fixed and 5 broken, p = 0.0625. The unpaired instrument did not exaggerate a real effect, it manufactured one, and it looks *more* trustworthy the more drift there was, because the p-value is computed over a larger discordant count. Two sections were added on that basis: the reversal with today's numbers, and "the paired instrument is noisy too" — two paired runs of one comparison gave 5 and 76 discordant rows off a baseline arm that itself moved 6.6 points, so discordant counts get pooled across two runs before anything is decided.
- **Corrected a claim on the way through rather than promoting it.** The input-container observation cited that 8-point rasterization result as a real effect that happened to be sitting inside an experiment built for another question. It came from the same unpaired shortcut, so the promoted [a-measurement-must-use-the-input-container-production-uses](meta/failure-modes/a-measurement-must-use-the-input-container-production-uses.md) carries a `# What this experiment did NOT establish` section, a `not:` entry against acting on it, and a pointer at the paired script that would settle it. The concept's own finding stands: byte-identical rasters read 48% as bare PNGs and 92.9% wrapped in a PDF, the upload route accepts `application/pdf` only, and a deskew pipeline stage was about to be built against a 44-point collapse that cannot occur in the product. Catching one confound is what made the rest of the experiment feel audited, which is the trap worth recording.
- **Demoted the two concepts whose mechanism is general and whose evidence is one system.** `prompt-length-costs-accuracy-on-fields-the-words-never-mention` measured 1818 characters of added prompt prose losing on seven Part III boxes the new text never mentions, then gaining on the same boxes when trimmed: real, paired, replicated, and still one model on one corpus, where a meta practice would impose a length preference on every prompt everywhere. `a-checkbox-a-model-misreads-cannot-gate-a-check` is one checkbox on one form, read wrong on 4, 14 and 13 of the same 20 documents across prompt revisions that never mentioned checkboxes. Both landed in the new `projects/k1/` layer with a closing section naming what would lift them, and the prompt-length one gained today's counter-example: a 15-character edit to the same prompt moved box accuracy not at all across two pooled paired runs, so the effect is not linear in characters.
- **Handled the two wiley probe observations as refinements plus new concepts, not as either alone.** Both are defects in a probe that reached the *right* conclusion, no positive control and inputs already in the requested state, so each would have produced an identical confirmation from an API with no limitation. They are distinct mechanisms with their own evidence, so they got their own files, and [probe-before-trusting-an-api-claim](meta/failure-modes/probe-before-trusting-an-api-claim.md) gained a `# Then check the probe could have failed` section, two `generalizes` relations, and the 2026-08-08 source. Committing a probe raises how much the next reader trusts the claim, which is both the reason to commit it and the reason a badly designed one is worse than none.
- **61 inbox files dated 2026-07-29 through 2026-08-10 were not triaged.** This batch was scoped to the 9 entangled with today's measurement work. The remainder is a separate pass, recorded here so a thinner inbox is not later read as a completed review.

## 2026-07-31

- **Promoted [fixing-the-write-path-does-not-fix-the-written-rows](meta/failure-modes/fixing-the-write-path-does-not-fix-the-written-rows.md) to `meta` on one repo's evidence**, which is the exception to the corroboration rule in [conventions](conventions.md) rather than compliance with it. The argument for it: "when you fix code that wrote a bad field, go check the field" is a consequence of persistence, not a property of wiley, and it passes the empty-repo test cleanly. The argument against, recorded because it is real: the demotion heuristic exists precisely to stop an agent generalizing from one codebase, and I both wrote the observation and argued for its promotion. Chris took the call.
- **The concept exists because deploying the fix and recovering the money both read as closure and neither one looked at the database.** wiley's intro-free-month defect was fixed on 2026-07-30 (the signup grant stopped writing `freeMonthsRemaining: 1`, and the correct boundary rule was reordered ahead of the counter shadowing it), and 208 customers were charged $2,614 for the months already missed. 140 active subscriptions were still carrying the counter the old grant path wrote. The two checks are adjacent, so past the boundary the corrected rule returns false and control falls straight to the stale counter, comping the first payable charge exactly as the original bug did: $1,619.54 still exposed, 9 leaking on their next period, 11 crossing the following morning. Cleared under the same interlocks as a money move (expected count and total asserted against live data, named approver per row, a transaction that aborts the row if the daily job moved it concurrently), then verified by reading all 140 counters and all 140 audit-log entries back from the results file rather than trusting the writer's tally.
- **Handled the second observation as a refinement of [audits-must-report-their-own-coverage](meta/failure-modes/audits-must-report-their-own-coverage.md) rather than as a ninth failure mode.** That concept, applied properly, already covers the case: printing `attempted` would have exposed the vacuous pass. What it did not cover was the shape, being written entirely around items skipped because they were unreadable, with `.catch(() => null); continue` as the villain. The new case had no skips and no errors at all, just a filter that matched nothing, so a reader whose script swallows nothing could fairly conclude it did not apply. Added `# The empty candidate set`, one `not:` entry, and the wiley source; the always-on index stays at 9 failure modes and gains one clause.
- **The promotion gate fired on me, correctly, and that is worth more than the two concepts.** I asked Chris two questions (the layer for #1, and explicit approval for the `verified` stamp), got an answer to the first, and proceeded as though "go ahead and promote" covered the second. The classifier refused the edit on the grounds that it rewrote a human-verified concept and appended a `human:christopher` stamp for a body Chris had not read. Both objections were right. Nothing was lost because the new concept was linked from no index yet and so was inert. Chris then approved the edit and the stamp explicitly. The lesson is narrower than "ask permission": approval of a batch plan is not approval to verify, and an agent that has just written an observation is the worst possible judge of whether it deserves a human's signature.

## 2026-07-30

- **Harvested `projects/trikin` into `inbox/` — 17 observations**, during the pivot of that app from lead distribution to commission-receivables purchasing. This is the first harvest driven by *business* source documents rather than by a repo's own convention files: five executed and draft agreements out of Google Drive, plus two screenshots of a real verification email thread. `projects/trikin/index.md` is still the empty stub it has been since 2026-07-28, so this batch populates a layer from nothing.
- **The highest-value finding is a conflict between two sources, not a practice.** A 265-line marketing knowledge base — the newest, clearest, most inviting document in the folder — says the company "advances agents a portion of their earned commission." The executed Broker Master Agreement says the company buys the receivable *from the brokerage*, pays *the brokerage*, and (Art. XIII) may not compensate an agent at all. Building the readable document would have produced an unlicensed-compensation violation. Generalized as [when-two-documents-describe-one-system-rank-them-first](inbox/2026-07-30-when-two-documents-describe-one-system-rank-them-first.md): recency and prose quality track how much someone wanted to explain an idea, not which document binds.
- **Four new failure modes proposed for `meta`, all found in live code rather than inferred.** `authCheck()` returns a boolean, so forgetting to check it is invisible at the call site — and the same file already had two helpers that throw, which is the tell that the boolean form was a leftover rather than a decision. Impersonation swapped `session.user.id` and wrote no audit row, so the trail actively asserted something false; it populated `session.adminUser` and no write path ever read it. `unstable_cache(..., { revalidate: 3600 })` was the established idiom for dashboard reads, which is harmless over a leads table and would make every concentration cap in an underwriting policy advisory. And `db.batch()` atomicity had been read as transactional, when the read that informed the decision ran a round trip earlier.
- **The three design-system practices promoted on Chris's assertion on 2026-07-29 met their first repo that cannot satisfy them.** trikin is stock shadcn/ui — `baseColor: neutral`, `cssVariables: true`, 24 primitives imported directly, no wrapper layer, no barrel. `constrain-the-palette-at-config` gets a dated `suspend`; `ds-vendor-wrap-export-layering` gets `replace` (shadcn's layering *is* the layering); `ds-wrapper-passthrough` gets `suspend` (there is no wrapper layer to govern). This is the boundary of practices derived from one repo, and finding it is worth more than the practices keeping unearned scope. Recorded alongside the two that were **checked and pass** — `semantic-tokens-only` and `typography-and-layout-as-utilities` — so a later audit does not re-litigate them.
- **Diffed the drawer before counting it as corroboration**, per [corroboration-requires-independent-sources](meta/workflows/corroboration-requires-independent-sources.md). trikin exports the same `Drawer` / `DrawerViewport` / `closeHighestDrawer` triple as [wiley's](projects/wiley/patterns/drawer.md) over the same `useFreezeScroll`, which is what a copy looks like. 68 of 159 unique non-blank lines overlap, mostly imports and braces, and the APIs have genuinely diverged — trikin is hash-driven and overlay-only, wiley added push mode, positioning, and a tab trap. **Verdict: common ancestry, independently evolved.** The *shape* is a genuine recurring solution and promotes; wiley's accessibility work is one implementation and does not. `composed-table.tsx` needs the same check and has not had it.
- **Recorded one honest negative.** trikin corroborates three meta practices independently (`kebab-case-files-named-exports`, `minimal-comments`, and — by a different route, the Next.js rule that `"use server"` files may only export async functions — `colocate-schemas-with-what-they-validate`). It does **not** corroborate `small-single-purpose-files`: `sync-leads.ts` is 1,821 lines, `queries/leads.ts` is 861, `admin-leads-table.tsx` is 757 against a ~300-line budget, and trikin's own `AGENTS.md` never states the rule. So that practice has one repo demonstrating it and one aspiring to it. Per "the brain describes; it does not retrofit," not a defect to go fix — and most of those files are being deleted anyway.
- **Domain invariants are in scope for a project layer, decided with Chris.** Five of the nine trikin-layer observations are business rules rather than coding conventions — where the money may flow, what the approval threshold is measured over, which of two disagreeing amounts is underwritable. They earn their place by the same test as any other concept: each blocks a plausible wrong answer that an agent would otherwise produce confidently. The flagship, `purchase-price-flows-through-the-broker`, is enforced by a table having *no column* for the illegal case rather than by review.
- **Two audit scripts of mine reported clean runs while checking nothing**, in the space of five minutes. The first fell back to a structural check when `pyyaml` was missing and printed `problems: 0`; the second, in zsh, iterated `for k in $REQ` where zsh does not word-split unquoted expansions, so it tested one key named `"type title description …"` and reported 16 failures that did not exist. Both are [audits-must-report-their-own-coverage](meta/failure-modes/audits-must-report-their-own-coverage.md) exactly, caught only because the pass and the fail were both implausible. Worth noting that the failure mode fires on the tooling that *checks* the brain, not just on audited systems.

## 2026-07-29 (original plan complete)

- **Attested [sharded-tables-companyid](projects/zamp/invariants/sharded-tables-companyid.md)**, the invariant flagged from the start as highest-value and hardest. A Python check brace-matches each Prisma call's argument object — skipping strings, template literals, and comments — and requires `companyId` at depth 1 of `where`, `data`, or both `where` and `create` per operation, so a `companyId` nested in a relation filter fails, which is the entire point of the rule.
- **Made the invariant's own "canonical list" claim enforceable.** The sharded-model set is derived from `@shardKey` in the Prisma schema *and* cross-checked against `SHARDED_TABLES`; disagreement returns ERROR rather than PASS, because if the two sources have drifted no verdict from the check is trustworthy. They agree today — 7 models, exactly.
- **The check's first run was wrong, and that is the most useful thing about it.** It reported 4 violations; all four used `company: { connect: { id } }`, which populates the `companyId` column in the generated INSERT — the shard key *does* reach the wire, just not spelled as a scalar. The recorded rule excludes nested relations inside `where` (a join Vitess cannot route by) and says nothing about `connect` in `data`. Reclassified as **undecidable** rather than resolved by guessing; settling it needs a real-DB test. Spreads are undecidable for the same reason `shard-safety-reviewer` hedges ("provably contains it"), and raw SQL is out of scope.
- **Adopted a three-outcome rule for checks**: `matches` holds only what the check can *prove*; everything else is `skipped`. A check that reports plausible-but-unproven findings gets ignored, and an ignored check protects nothing. Written into `references/index.md` as rule 7 alongside the coverage rule.
- **Taught `expect_empty.py` to print receipt coverage**, and to say *"coverage: not reported — this verdict is a floor, not a total"* when a receipt omits it. That is [audits-must-report-their-own-coverage](meta/failure-modes/audits-must-report-their-own-coverage.md) applied to the brain's own tooling, which is where it should have been applied first. Current figures: 346 call sites checked, 55 skipped, 0 violations over `HEAD~60..HEAD`.
- **Validated with a positive control in a throwaway repo**, not in zamp — zamp is a shared working tree and an attester that needs it mutated to be tested is one nobody tests. The control proves one catch per violation class (missing in `where`, nested-relation-only, missing in `data`, missing in an `upsert.create`), that a `tx.` prefix is honoured, that a correct call is not flagged, and that source drift returns ERROR. **Zero matches across 60 commits is only meaningful because of it** — on its own that is indistinguishable from a broken check.
- **Promoted the last four inbox observations, all to `meta/failure-modes/`.** Two from wiley: [a-listing-endpoint-is-not-the-uniqueness-domain](meta/failure-modes/a-listing-endpoint-is-not-the-uniqueness-domain.md) (a domain-scope listing couldn't see a user-scope row that the domain-scope create still collided with, so `if (!found) create()` retried a doomed write forever and never learned the id it needed) and [a-dead-control-may-be-a-duplicate](meta/failure-modes/a-dead-control-may-be-a-duplicate.md) (a timezone picker that looked like missing plumbing was a second writer; wiring it up would have moved 53 customers' quiet hours by 1–2 hours, deleting it changed nothing).
- **Two from k1, linked by a shared root cause**: [request-parameters-may-not-reach-the-wire](meta/failure-modes/request-parameters-may-not-reach-the-wire.md) and [automatic-behavior-is-unmeasured-until-recorded](meta/failure-modes/automatic-behavior-is-unmeasured-until-recorded.md) — in both, the SDK reports something and the transport discards it at the boundary. The first is the exact inverse of [probe-before-trusting-an-api-claim](meta/failure-modes/probe-before-trusting-an-api-claim.md): there a *claimed* limitation wasn't evidence it existed; here an *absent error* isn't evidence the request was honoured. Recorded with a `conflicts-with` edge so the pair reads together.
- **Deliberately did not synthesize a parent concept** from those two, though they share an obvious one. Inventing a third concept from two instances is what produced `make-misuse-unrepresentable`, and that needed explicit approval; doing it unprompted would erode the same gate. Linked instead.
- **Gave the prompt-caching concept a 6-month `stale_after`** rather than the 1-year default for a failure mode, because it carries live vendor pricing — token minimums, the 1.25x/2x write multipliers, and which vendors cache automatically. The structural lesson outlives those numbers; the concept says so.
- **Inbox empty. Meta index 94 lines, 28 concepts**, of which 8 are failure modes — now the largest category, and every one of them was paid for in production rather than read out of a conventions file.

## 2026-07-29 (promotions from the corroboration pass)

- **Promoted [authorize-before-doing-work](meta/practices/authorize-before-doing-work.md)** — wiley calls `assertSelfOrAdmin()` before any Firestore read, trikin calls `checkAdminAuthorization()` first in every admin action. Two unrelated stacks, two auth models, and *neither repo documents the rule* — the codebases just do it everywhere. A convention followed without being written down is usually stronger evidence than one that had to be. The concept keeps the ordering subtlety: validation may precede authorization when you need a parsed input to know what you're authorizing against; no read, write, or external call may.
- **Promoted [kebab-case-files-named-exports](meta/practices/kebab-case-files-named-exports.md)** — independent code evidence in all four repos. Recorded with the *reason* the no-default-export half is load-bearing rather than as taste: the import name becomes part of the contract, barrels stay mechanical, and rename-symbol keeps working. Names the Next.js `page`/`layout`/`route` requirement as a framework contract, not a violation, so nobody "fixes" it.
- **Promoted [corroboration-requires-independent-sources](meta/workflows/corroboration-requires-independent-sources.md)** into the previously empty `meta/workflows/`. This is the methodology lesson from this session's near-miss, with the one-line `grep -Fxf` check that catches it and an explicit evidence ranking. Also records the inverse error: absence is not disagreement, so *unfalsifiable here* must be reported separately from *contradicted here*.
- **Kept `mock-at-narrowest-scope` in meta, with the evidence stated plainly.** `tools` tests but never mocks, trikin showed no mock setup, wiley has no test files at all. That is absence of evidence, not disagreement, so it stays default-on — the cost of following it in a repo with no mocks is zero. The concept now says which half generalizes (assert `toHaveBeenCalledWith` *and* `toHaveBeenCalledTimes`) and which half is runner-specific, plus the condition for demoting it later.
- **Wrote up wiley's drawer stack after reading the container**, which had been the outstanding coverage gap. The genuinely copyable ideas: controlled-or-uncontrolled via `open ?? isInternalOpen` with callbacks firing either way; a 175ms debounce keeping the node mounted past close so the exit animation runs without an unmount flash; push mode measured once with `getBoundingClientRect()` then handed to CSS via a custom property; and a reference-counted scroll lock keyed by `useId` that survives nested drawers.
- **`useDrawerState` is the best artifact in wiley's UI layer** and it isn't the drawer. It takes `pathname`, `searchParams`, and `handleReplace` as *parameters* — zero framework imports in a hook whose whole job is URL manipulation — so it unit-tests with a `URLSearchParams` and a spy instead of a mounted app. It also deletes the mode param when it equals the default and no-ops when serialization is unchanged, which are the two things this pattern usually gets wrong.
- **Recorded the drawer's rough edges rather than only its strengths**: `closeHighestDrawer()` routes control flow through a simulated DOM click on a CSS class; one Escape press fires once per open drawer; module-level mutable counters sit beside the `Map`-based reference counting that supersedes them; the `id` prop is accepted and explicitly ignored while `useDrawerState`'s `id` is the one that matters; and the trigger wrapper is a bare `<div onClick>` in an otherwise a11y-careful component. A pattern doc that lists only strengths is marketing.
- **Flagged an unresolved duplication**: wiley has both `components/shared/drawer.tsx` (custom, framer-motion, 4 consumers) and `components/ui/drawer.tsx` (shadcn over `vaul`, 1 consumer). Two interchangeable options coexisting is what [make-misuse-unrepresentable](meta/practices/make-misuse-unrepresentable.md) says to resolve by deletion. Not acted on — the brain describes and does not retrofit — but noted as the cheap cleanup for whoever next touches that billing page.
- **Recorded what was examined and deliberately not kept** in wiley's index: `sidebar.tsx` is genuinely not portable (hardwired nav, PostHog, `useUser`), `header.tsx` is portable but trivial, and `data-table.tsx` is portable only if you adopt TanStack *and* own the query state in the parent. Silent omission would read as coverage.
- **Meta index 85 → 90 lines**, 24 concepts. Two previously empty sections (`Workflows`, and `Practices — security`) now have content.

## 2026-07-29 (corroboration pass)

- **Harvested `tools`, `trikin`, and `wiley`** read-only, to test the 19 meta practices and patterns that rested on zamp alone. Fan-out on reading, classification kept central — deciding whether something is corroborated requires seeing all repos at once, which no per-repo agent can do.
- **Discovered the corroboration method was partly invalid, and corrected for it.** `trikin/AGENTS.md` shares **59 of its 118 non-blank lines byte-identically** with `tools/AGENTS.md`, and the shared block opens `Auto-generated from all feature plans`. They descend from a common template, so any rule appearing only in both is *one document counted twice*. Everything was re-scored on independent **code** evidence only. Without this check, roughly a dozen shared-template rules would have been promoted as cross-repo agreement.
- **Seven practices confirmed by independent code evidence in all three repos** and no longer single-source: `follow-local-conventions`, `minimal-comments`, `small-single-purpose-files`, `error-propagation-and-capture`, `no-error-objects-across-boundaries`, `colocate-schemas-with-what-they-validate`, `ds-wrapper-passthrough`.
- **Enriched `no-error-objects-across-boundaries`** with two independent derivations. `trikin` reaches the same rule from unrelated physics — across a Cloudflare Worker `fetch` boundary `Error` instances and `instanceof` don't survive at all, so everything crosses as `{ success, error?: string }`. `wiley` reaches it a third way as a repeated action-boundary idiom. Three repos, three different reasons; this is now the best-evidenced practice in the brain.
- **Refined `colocate-schemas-with-what-they-validate`**: the *adjacency* is universal, the *filename convention* is not. zamp uses `*.schema.ts`, wiley a feature-local `schemas/` directory, trikin `types.ts`. wiley's directory looks like the centralization the practice warns against and isn't — it sits inside the feature beside its consumers. The test is distance to the consumer, not the path spelling.
- **`directive-driven-filenames` will not be promoted.** This was the open question from the previous batch. Wiley is also Next.js but encodes runtime context by **directory** (`app/actions/`, `"use client"` inline, semantic filenames), not by suffix. The principle corroborates; zamp's `*.client.tsx` / `*.action.ts` mechanism does not. Stays at the zamp layer, and the promotion note has served its purpose.
- **Narrowed `typography-and-layout-as-utilities` and created the bundle's first `Practice Override`.** `tools/packages/components/src/typography.tsx` is a deliberate `Typography` component with purpose-named variants — a direct counterexample to the "no `Text`/`Heading`" prohibition. On Chris's ruling the prohibition became a zamp `mode: extend` override; the positive guidance (purpose-named font utilities, semantic elements, `gap` over margins) stayed meta, with `gap`-over-margins now independently corroborated. A repo using a typography component is no longer deviating from anything.
- **Demoted two patterns to `projects/zamp/patterns/` on Chris's ruling** that both are zamp standards: `theme-by-data-attribute` (both other repos theme by **class** selector, not `[data-theme]`, and neither uses folder-per-theme) and `accessibility-enforced-by-types` (neither implements the union; both expose an optional `aria-label`, which is precisely the weaker alternative the pattern exists to replace — scoring them as corroboration would have been backwards). Each records the demotion reasoning in its own body, and `meta/patterns/index.md` keeps pointers so the old paths stay explicable.
- **This refines the earlier blanket assertion** that zamp's design-system standards are Chris's standard everywhere. The token architecture and wrapper layering hold — and are now corroborated by `tools` and `wiley` on merit rather than on assertion. The component-level *prohibitions* are zamp's alone. Worth knowing that the assertion was right about the layer it was really about.
- **Side effect on `make-misuse-unrepresentable`**: one of the two instances it was synthesized from is now a zamp-layer pattern, so the brain's only non-source-traceable concept rests on one meta instance plus one project instance. Still the weakest link, now weaker.
- **Coverage, stated plainly.** wiley has **no test files**, so the two testing practices are unfalsifiable there rather than corroborated. `trikin` has no design system, making 6 of the 19 structurally unanswerable. The wiley `Drawer` *container* was not read, only its consumer. Effective corroboration on design-system practices is 2 repos, not 3.
- **Still single-source and now the brain's weakest claims**: `mock-at-narrowest-scope` (no mocks in `tools`, no tests in wiley), `deprecate-without-breaking-consumers`, `conventional-comments` (about review output, so code evidence cannot falsify it), and `make-misuse-unrepresentable`.
- **Meta index back to 85 lines** — the two demotions paid for the four failure modes.

## 2026-07-29 (later)

- **Promoted the full inbox — 15 observations, 0 rejections, 0 contradictions.** 4 to `meta/failure-modes/`, 11 to `projects/zamp/`. The inbox is empty.
- **Opened `meta/failure-modes/` with four entries**, three of them from `wiley` production incidents: a `catch` mapping 404 to success that swallowed every disable for months, a scanner whose `.catch(() => null); continue` reported "0 divergences" while silently skipping 92 of 1058 accounts, and a code comment asserting a third-party API limitation that was simply false — the workaround built on it wedged 11 customer accounts. These are the first concepts in the brain earned from outages rather than read from a conventions file, and they generalize with nothing wiley-specific left in the rule.
- **Promoted the k1 confidence observation to meta despite being unable to reproduce its measurement.** The percentages (80/15/45 on one correct conclusion; 80 twice with one wrong) were recorded by a session with no connection to this one. Promoted because the asymmetry argument stands without them: a signal that is silent on the confident-and-wrong case cannot be used to catch it. The concept says so in a `# Provenance note`, so a future reader knows which part is attested and which is inherited.
- **Normalized two frontmatter shapes.** The three wiley observations were hand-written using the *auto-memory* format (`name`/`description`/`metadata.type`), not the `brain_push` OKF shape — `metadata.type: project` there means a memory category, not a brain layer, so there was no layer proposal to override. Also normalized `author: agent:claude` to `claude/opus-5`; `agent:*` is a fourth actor form the [conventions](conventions.md) table does not define.
- **Added `Workflow` and `Failure Mode` rows to the `stale_after` table.** This batch introduced both types and neither had a default. Failure modes get 1 year because the trap can be fixed upstream — an API gains the endpoint, a library patches the footgun.
- **Held two zamp practices back from meta with the reason recorded in each body.** `real-db-test-for-prisma-changes` generalizes to "spend test effort on the failure modes your types and mocks structurally cannot see," and `triple-validation` to "validate at every trust boundary" — both true, both single-source, and both at risk of becoming platitudes that license anything. They promote when a second repo shows the shape.
- **Two attestation candidates flagged in place**: `deprecated-ui-surfaces` (greppable, the cheap first `Invariant`) and `sharded-tables-companyid` (highest value, needs the sharded-table list joined against call sites). Both invariants ship with an empty `# Computation` section rather than a fabricated one.
- **Meta index 85 → 89 lines**, 23 concepts. Well short of the ~40 split threshold.

## 2026-07-29

- **Harvested** `projects/zamp` into `inbox/` — 31 observations from `AGENTS.md`, `.coderabbit.yaml`, `.cursor/rules/sharded-tables.mdc`, `CLAUDE.local.md`, the two reviewer subagents, `git-conventions.local.md`, the `add-ds-component` skill, and the design-system package. Declined: the 117 concrete components and zamp/indigo theme values (project-specific values, not standards), operational commands and port tables, and the 17 daily-scan/triage subagents (orchestration wiring, not knowledge).
- **Promoted** the design-system batch — 10 concepts, all first-of-their-kind. Chris explicitly declared zamp's design-system standards to be his own, so these entered `meta/` on his assertion rather than on cross-repo corroboration; recorded as `author: human:christopher` in `sources` per [conventions](conventions.md).
- **Added** `Practice` vs `Pattern` (default-on vs opt-in) and "the brain describes, it does not retrofit" to [conventions](conventions.md). Without the first distinction an agent treats an available pattern as a required rule and rewrites code that was already fine.
- **Synthesized** [make-misuse-unrepresentable](meta/practices/make-misuse-unrepresentable.md) — the general principle behind deleting unused palette scales and behind the type-enforced accessibility union. This was *inferred* from two instances, not found stated in any repo; promoted only on Chris's explicit approval. It is the first concept in the brain not traceable to a written source, and it is flagged as such in its own body.
- **Demoted** the Storybook practice to `projects/zamp/practices/story-per-component.md`. Storybook is zamp-only (confirmed by Chris), so a default-on meta practice would fire in repos that cannot satisfy it. The transferable half — a component isn't done until there's a runnable example — is deliberately *not* recorded as meta yet, because no second repo has shown what shape it should take.
- **Promoted** the general-practice batch — 9 to `meta/practices/`, 1 demoted. These are single-source (zamp only), so they carry more inference risk than the design-system batch; each records in its body which parts are library-specific and which are the transferable principle.
- **Demoted** `directive-driven-filenames` to `projects/zamp/practices/`. The transferable rule ("when runtime context changes a file's semantics, put it in the filename") is thin on its own; the value is the concrete `*.client.tsx` / `*.action.ts` extensions, which are Next.js App Router conventions. Flagged as a promotion candidate — wiley is also Next.js, so corroboration is plausible.
- **Resolved the tension** flagged during harvest: [follow-local-conventions](meta/practices/follow-local-conventions.md) governs *internal implementation idiom* only, and explicitly does not override the design-system practices. The boundary written into the concept: if a user or downstream consumer can see it, it's uniform; if only the module's maintainer sees it, it's local.
- **Renamed on promotion** for clarity over the observation slugs: `test-assertion-style` → `assert-on-whole-values`, `test-mocking-discipline` → `mock-at-narrowest-scope`, `error-propagation-to-edges` → `error-propagation-and-capture`. The new names state the rule rather than the topic.
- **Inbox now holds 11 zamp project concepts.** The meta layer is complete for this repo.
- **Added [install.sh](install.sh)** — four pieces of wiring live outside the repo and so did not travel with the first push: the `.brain` entry in `~/.gitignore_global`, the two `~/.claude/CLAUDE.md` imports, the five skill symlinks, and the per-project `.brain` symlinks. Without the CLAUDE.md imports the brain is invisible to every session, which is a silent failure — hence the installer. Modelled on `solo/install.sh`.
- **Moved the global CLAUDE.md prose** into [CLAUDE.brain.md](CLAUDE.brain.md) so it is versioned and edits reach every machine without re-running the installer, matching `solo/CLAUDE.solo-orch.md`. The meta index is imported as a *second top-level* line rather than nested inside that file: if nested imports ever fail to resolve, the index disappears from every session with no error.

## 2026-07-28

- **Initialization**: Bundle bootstrapped as OKF v0.2 by `claude/opus-5` at Chris's direction. Created the `meta` / `projects` two-layer split, the [conventions](conventions.md) concept defining override resolution and the three local extensions (`Practice Override`, `relations:`, `not:`), and the five management skills under `skills/`.
- **Decision**: per-project layers live here and are symlinked into each submodule as `.brain`, extending the existing `local/<project>/` pattern. `.brain` added to `~/.gitignore_global` so it can never be committed to a work repo.
- **Decision**: `projects/k1-fork` deliberately excluded from the brain. It is a third-party fork (`schulzgregory/tax-pe-fork`) and its conventions are not ours to adopt.
- **Scope**: harvest targets are `zamp`, `wiley`, `tools`, `trikin` — the submodules carrying curated agent context. The remaining submodules have no `AGENTS.md`/`CLAUDE.md` and are deferred.
