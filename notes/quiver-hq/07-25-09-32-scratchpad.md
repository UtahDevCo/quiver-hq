# 07-25-09-32 Scratchpad

## SoloTerm Notes

### Linux

I don’t need much, just an executable. A little copy/pastable bash command or even a copy/paste LLM command that I can give directly to Claude. I’ll give those commands to Claude and let it handle the installation.   &#x20;

### Scrolling

The scrolling is a little slow. I scroll up and down a lot and it’s not native-level fast. This might not be changeable… but it’s worth at least checking.&#x20;

### Typing jank

Something about typing in this field is kind of slow and the cursor is occassionally jumping back into the middle of the world that I just typed. It only happens occasionally, and I think it might have something to do with the auto-complete. I’m on a mac. It usually happens after I use a contraction (').

For example, if I type “I’d like to type something quickly”, it’ll often type like “I’ like to type something quicklyd"&#x20;

### Using hashes

I want to be able to type “## some title” and have it auto-complete as “\<h2>some title\</h2>"

### Images

I should be able to copy/paste local images into this scratchpad

### VSCode launch

I created a command to launch vscode across both of my machines: \`code-insiders . || code .\` I copied that to all of my workspaces so I can quickly launch vscode.

&#x20;&#x20;

### Exit

When I type \`exit\` into my terminals they don’t close like I’d expect. &#x20;
