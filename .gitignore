# Binaries
bin/*
!bin/quiver-monitor
!bin/gh-stacks
controller
quiver-secrets
interactive-mission
risky-mission
test-mission
list-models
test-manager

# Database
quiver.db
quiver.db*

# Environment files
.env
.env.local
.env.development
.env.test

# Test artifacts
test-project/

# Nix/Direnv
.direnv/
result
result-*

.npm-global/
.bin/
projects/ts-interview-skeleton/
.DS_Store

# Foundation projects — live untracked on this machine (orphaned submodules)
projects/foundation-integrations/
projects/foundation-web/

# Working data. Syncs between machines via quiver-sync (rsync) and is never
# committed — it holds customer data and ages out by hand.
# (scratch/agy predates this rule and stays tracked.)
temp/
scratch/
